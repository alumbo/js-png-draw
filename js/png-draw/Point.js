Point = {
    /* PUBLIC VARIABLES */
    x:0,
    y:0,
    r:0,
    g:0,
    b:0,
    a:255,
    
    /* PUBLIC METHODS */
    init:function(x,y,r,g,b,a) {
        if(x!=undefined) this.x = x;
        if(y!=undefined) this.y = y;
        if(r!=undefined) this.r = r;
        if(g!=undefined) this.g = g;
        if(b!=undefined) this.b = b;
        if(a!=undefined) this.a = a;
    }
   
    /* PRIVATE VARIABLES */
   
    /* PRIVATE METHODS */
}
